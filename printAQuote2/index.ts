const famousQuote: string = "Be yourself; everyone else is already taken.";
const author: string = "Oscar Wilde";
const message: string = `${author} once said, “${famousQuote}”`;
console.log(message);
