let myFavoriteTransportation: string[] = ['car', 'motorcycle', 'bike', 'plane', 'boat'];

myFavoriteTransportation.forEach((transportation) => {
    console.log(`I would like to own a ${transportation}`);
}
);